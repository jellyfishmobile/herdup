{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only what herdup's window actually uses: core defaults, event listening for launch progress, and the folder picker. No filesystem, shell or HTTP permissions are granted — everything that touches the machine goes through launcher-core, not the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open"
  ]
}
